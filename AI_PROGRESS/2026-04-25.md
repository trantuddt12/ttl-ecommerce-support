### 2026-04-25 - Shared - Documentation-only validation rule

- Scope: shared
- Changed:
  - `ecommerce-shop/AGENTS.md`
  - `ecommerce-shop/.ai/BASELINE.md`
  - `ecommerce-shop/.ai/AI_CONFIGURATION_GUIDE.md`
  - `ecommerce-shop/.ai/instructions/docs-sync.instructions.md`
- Summary:
  - Clarified that documentation-only edits do not require code build or test commands.
  - Set changed-content review as sufficient validation unless executable code, config, API contracts, or run instructions are affected.
- Verification:
  - Reviewed changed markdown content only; no build/test run because this was documentation-only guidance.
- Todo:
  - [ ] Keep this rule aligned if future AI workflow docs add separate validation gates.
- Notes:
  - Applies to document edits only, not code/config changes.

### 2026-04-25 15:59 - Infra - RedisInsight compose service

- Scope: infra
- Changed:
  - `ecommerce-shop/docker-compose.yml`
  - `ecommerce-shop/basecommerce/src/main/resources/docker-ecommerce/docker-compose.yml`
- Summary:
  - Added a RedisInsight service exposed on host port `5540` with a persistent `redisinsight_data` volume.
  - Added RedisInsight to the ecommerce infrastructure compose file with a Redis health dependency.
- Verification:
  - `docker compose config` passed; Docker Compose warned that the root compose top-level `version` attribute is obsolete.
  - `docker compose -f basecommerce/src/main/resources/docker-ecommerce/docker-compose.yml config` passed.
- Todo:
  - [ ] Run `docker compose -f basecommerce/src/main/resources/docker-ecommerce/docker-compose.yml up -d redisinsight` and open `http://localhost:5540`.
- Notes:
  - RedisInsight can connect to the compose Redis service using host `redis` and port `6379` inside the Docker network.

### 2026-04-25 08:47 - Backend - Service error line logging

- Scope: backend
- Changed:
  - `ecommerce-shop/coreservice/src/main/java/com/ttl/core/aspect/ServiceLoggingAspect.java`
- Summary:
  - Enhanced service exception logging to include the originating application class and source line number.
  - Keeps the full exception stack trace in the same log entry.
- Verification:
  - `mvnw.cmd -pl coreservice -DskipTests compile` failed because the current environment is running a JRE without a JDK compiler.
- Todo:
  - [ ] Re-run compile/test with a JDK configured in JAVA_HOME.
- Notes:
  - No API behavior change.

### 2026-04-25 10:16 - Frontend/Backend - Home products access fixes

- Scope: frontend, backend
- Changed:
  - `ecommerce-ng/src/app/core/layout/client-layout.component.ts`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/controller/BrandController.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/controller/CategoryController.java`
  - `ecommerce-shop/coreservice/src/main/java/com/ttl/core/config/SecurityConfig.java`
- Summary:
  - Deferred cart loading from `ClientLayoutComponent` effect with `queueMicrotask` to avoid Angular NG0600 signal writes inside an effect.
  - Allowed public reads for `/brands`, `/categories`, and `/categories/tree` used by `/home/products` while keeping write/admin endpoints protected.
- Verification:
  - `npm run test -- --watch=false --browsers=ChromeHeadless` passed: 2 specs successful.
  - `npm run build` still fails on an existing Angular CSS budget error in `src/app/features/catalog/products/products.page.ts` (4.71 kB > 4.00 kB); no TypeScript compile error from this change was reported.
  - `mvnw.cmd -pl basecommerce test` blocked because current `java -version` is Java 8 JRE without a JDK compiler; backend requires Java 17/JDK.
- Todo:
  - [ ] Re-run backend tests with JDK 17 configured in JAVA_HOME.
  - [ ] Decide whether to reduce `products.page.ts` CSS or adjust Angular budgets.
- Notes:
  - Existing unrelated/unowned working-tree changes were left untouched.

### 2026-04-25 10:28 - Frontend - Home category tree query fix

- Scope: frontend
- Changed:
  - `ecommerce-ng/src/app/features/public/public-products.page.ts`
- Summary:
  - Fixed the home products category tree request to send backend-supported `visibleOnly=true` instead of unsupported `visible=true`.
  - This makes `/categories/tree` filter visible active categories instead of returning an unfiltered/empty tree for the storefront block.
- Verification:
  - `npm run test -- --watch=false --browsers=ChromeHeadless` passed: 2 specs successful.
- Todo:
  - [ ] Re-test `/home/products` in browser after restarting/reloading frontend and backend.
- Notes:
  - Backend controller already expects `visibleOnly` and `assignableOnly` for tree requests.

### 2026-04-25 10:31 - Frontend - Resilient home category and brand loading

- Scope: frontend
- Changed:
  - `ecommerce-ng/src/app/features/public/public-products.page.ts`
- Summary:
  - Made home data loading resilient so failure from products, category tree, category list, or brand list no longer cancels all other home requests.
  - Added fallback tree construction from `/categories` when `/categories/tree` returns empty, allowing category DB rows to still render in the sidebar.
- Verification:
  - `npm run test -- --watch=false --browsers=ChromeHeadless` passed: 2 specs successful.
- Todo:
  - [ ] Reload `/home/products` and verify Network tab responses for `/brands`, `/categories`, and `/categories/tree`.
- Notes:
  - If a category still does not show, confirm its DB row has `deleted=false`, `status=ACTIVE`, and `visible=true`.

### 2026-04-25 14:00 - Frontend/Backend - Public storefront products endpoint

- Scope: frontend, backend
- Changed:
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/controller/ProductController.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/service/ProductService.java`
  - `ecommerce-ng/src/app/core/constants/api-endpoints.ts`
  - `ecommerce-ng/src/app/core/services/product-api.service.ts`
  - `ecommerce-ng/src/app/features/public/public-products.page.ts`
- Summary:
  - Added public `GET /products/storefront` for active catalog-visible storefront products while keeping admin `/products` protected by `PRODUCT_VIEW`.
  - Updated `/home/products` to load products through the new storefront API instead of the protected admin list.
- Verification:
  - `npm run test -- --watch=false --browsers=ChromeHeadless` passed: 2 specs successful.
  - `npm run build` still fails on existing Angular CSS budget error in `src/app/features/catalog/products/products.page.ts` (4.71 kB > 4.00 kB); generated bundles before budget failure show no TypeScript error from this change.
  - `mvnw.cmd -pl basecommerce -Dtest=ProductServiceTest test` blocked because current environment has a JRE without a JDK compiler.
- Todo:
  - [ ] Re-run backend tests with JDK 17 configured in JAVA_HOME.
  - [ ] Restart backend and reload `/home/products`; verify `GET /products/storefront`, `/categories`, and `/categories/tree` return 200.
- Notes:
  - Existing unrelated working-tree changes were left untouched.

### 2026-04-25 14:35 - Frontend - Tolerant category visibility mapping

- Scope: frontend
- Changed:
  - `ecommerce-ng/src/app/features/public/public-products.page.ts`
- Summary:
  - Relaxed storefront category filtering so categories with missing/undefined `visible` values from `/categories/tree` or `/categories` are still rendered when `status=ACTIVE`.
  - Normalized tree children defensively when backend returns empty or partially shaped category tree rows.
- Verification:
  - `npm run test -- --watch=false --browsers=ChromeHeadless` passed: 2 specs successful.
  - `npm run build` still fails on existing Angular CSS budget error in `src/app/features/catalog/products/products.page.ts` (4.71 kB > 4.00 kB); no TypeScript error from this change.
- Todo:
  - [ ] Reload `/home/products` and hard-refresh the Angular dev server bundle.
  - [ ] If categories still do not show, inspect Network response for `GET /categories?status=ACTIVE&visible=true` and `GET /categories/tree?status=ACTIVE&visibleOnly=true`.
- Notes:
  - This keeps explicit `visible=false` hidden, but no longer hides rows just because `visible` is omitted by the backend response.
