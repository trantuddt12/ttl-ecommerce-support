# 2026-05-09

## gRPC architecture assessment

### Scope: docs

#### Changed files
- `ecommerce-shop/docs/grpc-architecture-assessment.md`

#### Summary
Đánh giá toàn diện khả năng áp dụng gRPC vào `ecommerce-shop`, chốt hướng dùng gRPC có chọn lọc cho internal `searchservice -> basecommerce` catalog source pull, giữ REST public API và Kafka event flow, đồng thời mô tả impact lên module ownership, contract, security, observability, testing, và migration phase.

### Verification
Manual review markdown, đối chiếu module structure hiện tại, kiểm tra path internal REST mismatch, và chạy `git diff --check` trên file mới.

### Risks
Chưa thay code runtime; doc mới là định hướng kiến trúc và cần follow-up implementation nếu quyết định áp dụng.

### Next todos
- [ ] Chốt location `.proto` và module contract ownership
- [ ] Viết ADR ngắn cho gRPC fallback/security/migration
- [ ] Benchmark REST vs gRPC cho product search source pull

## Java core multithreading file I/O assessment doc

### Scope: docs

#### Changed files
- `ecommerce-shop/docs/java-core-multithreading-fileio-senior-assessment.md`

#### Summary
Thêm mục “Vì sao cần / nếu không có thì sao” vào từng chủ đề theo style self-assessment microservice, để mỗi topic có trade-off và failure mode rõ hơn ngoài Evidence/Gap/Ví dụ.

### Verification
Manual review markdown.

### Risks
Nội dung lặp cùng một câu khung ở nhiều mục; nếu cần có thể rút gọn lại sau.

### Next todos
- [ ] Rà lại wording từng mục nếu muốn giảm lặp
- [ ] Tách vài ví dụ code dài ra file riêng nếu tài liệu quá nặng

## Event architecture review doc

### Scope: docs

#### Changed files
- `ecommerce-shop/plans/plan-event-architecture-review.md`

#### Summary
Thêm mục “Vì sao cần / nếu không có thì sao” vào plan event architecture để đồng bộ format với docs self-assessment microservice, giúp mỗi issue có trade-off và failure mode rõ hơn.

### Verification
Manual review markdown, `git diff --check`.

### Risks
Nội dung mới theo cùng pattern nên có thể trùng ý ở vài issue; nếu muốn có thể tinh gọn sau.

### Next todos
- [ ] Rà lại wording nếu muốn giảm lặp
- [ ] Tách plan lớn thành phần issues/fix riêng nếu tài liệu tiếp tục phình
