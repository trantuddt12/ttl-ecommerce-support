# 2026-05-18

### 2026-05-18 09:35 - Backend/Frontend - Simulated MoMo checkout flow

- Scope: shared
- Changed:
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/payment/controller/PaymentController.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/payment/domain/PaymentIntent.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/payment/dto/request/MomoPaymentSimulationRequest.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/payment/dto/response/MomoPaymentSimulationResponse.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/payment/service/PaymentService.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/service/OrderService.java`
  - `ecommerce-shop/basecommerce/src/test/java/com/ttl/base/payment/domain/PaymentIntentDomainTest.java`
  - `ecommerce-shop/basecommerce/src/test/java/com/ttl/base/payment/service/PaymentServiceTest.java`
  - `ecommerce-shop/docs/payment-momo-simulation.md`
  - `ecommerce-ng/src/app/core/constants/api-endpoints.ts`
  - `ecommerce-ng/src/app/core/constants/app-routes.ts`
  - `ecommerce-ng/src/app/core/models/payment.models.ts`
  - `ecommerce-ng/src/app/core/services/payment-api.service.ts`
  - `ecommerce-ng/src/app/features/client/client.routes.ts`
  - `ecommerce-ng/src/app/features/order/checkout/checkout.page.ts`
  - `ecommerce-ng/src/app/features/order/momo-payment/momo-payment.page.ts`
  - `ecommerce-ng/src/app/features/order/my-order-detail/my-order-detail.page.ts`
- Summary:
  - Added authenticated `POST /payments/intents/{id}/momo/simulate` for local MoMo approval/decline simulation.
  - MoMo checkout now creates a payment intent and waits for customer confirmation instead of starting the async saga; approved simulation authorizes, captures, confirms the order, reserves inventory, and records payment/order history.
  - Checkout UI now offers COD vs MoMo, redirects MoMo orders to `/client/payments/momo/:orderId`, and exposes a MoMo sandbox page with approve/decline actions.
- Verification:
  - `.\mvnw.cmd -pl basecommerce -am "-Dtest=PaymentIntentDomainTest,PaymentServiceTest,OrderServiceBehaviorTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` -> passed, 37 tests.
  - `npx tsc -p tsconfig.app.json --noEmit` -> passed.
  - `npx ngc -p tsconfig.app.json --noEmit` -> passed.
  - `Invoke-WebRequest` against existing dev server returned HTTP 200 for `/client/checkout` and `/client/payments/momo/1`.
  - `npm run build` and `npx ng build --configuration development --progress=false` timed out without returning logs; leftover build processes were stopped. Existing `ng serve` on `localhost:4200` remained running and `/client/checkout` returned HTTP 200.
- Todo:
  - [ ] Manual browser QA with a real cart: COD checkout, MoMo checkout approve, MoMo decline/retry, and order detail status.
  - [ ] Decide later whether real online providers should use the async Kafka saga or the customer-return payment page pattern.
- Notes:
  - Existing untracked `ecommerce-shop/walkthrough.md` was left untouched.

### 2026-05-18 10:18 - Frontend - COD payment admin handling

- Scope: frontend
- Changed:
  - `ecommerce-ng/src/app/features/order/admin-order-detail/admin-order-detail.page.ts`
- Summary:
  - Reviewed COD payment flow against backend `OrderController`/`OrderService`: COD remains `UNPAID` until admin manually updates payment status through `/orders/{id}/payment-status` or the combined admin status endpoint.
  - Split admin order detail UI so order/fulfillment updates no longer submit `paymentStatus` through the generic `/admin-status` form.
  - Added a dedicated `Thanh toan COD` section that calls `OrderApiService.updateAdminPaymentStatus()` and includes a quick `Da thu COD` action that sets `SETTLED`.
  - Gated mutation controls with `ORDER_UPDATE`/`ORDER_MANAGE`; users with only `ORDER_VIEW` can still view detail but no longer see editable controls.
- Verification:
  - `.\node_modules\.bin\tsc.cmd -p tsconfig.app.json --noEmit` from `ecommerce-ng/` -> passed.
  - `.\node_modules\.bin\ngc.cmd -p tsconfig.app.json --noEmit` from `ecommerce-ng/` -> passed.
  - `npm run build` could not start because global npm is missing `npm-cli.js`.
  - `.\node_modules\.bin\ng.cmd build --configuration development --progress=false` timed out at 300s after only existing Angular SSR `withFetch()` warnings; no compile error was emitted before timeout.
  - `Invoke-WebRequest http://localhost:4200/admin/orders/1` -> HTTP 200 from the existing dev server.
- Todo:
  - [ ] Manual browser QA with an admin account: open a COD order at `/admin/orders/:id`, click `Da thu COD`, confirm paymentStatus becomes `SETTLED`, and verify an `ORDER_VIEW`-only account sees read-only controls.
- Notes:
  - `ecommerce-ng/` is ignored by root `.gitignore`, so root `git diff/status` does not show this frontend file change.

### 2026-05-18 15:25 - Backend - Async checkout saga command consumers

- Scope: backend
- Changed:
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/saga/consumer/SagaCommandConsumer.java`
  - `ecommerce-shop/basecommerce/src/main/java/com/ttl/base/saga/service/SagaCommandHandlerService.java`
  - `ecommerce-shop/basecommerce/src/test/java/com/ttl/base/saga/service/SagaCommandHandlerServiceTest.java`
  - `ecommerce-shop/docs/order-saga-pattern-design.md`
- Summary:
  - Added Kafka command listeners for `order.saga.command.*` checkout saga topics so outbox-published commands are now consumed and converted into reply events.
  - Added transactional command handling for inventory reserve/release, payment authorize/capture/void, and order confirm/cancel, reusing `OrderInventoryService`, `PaymentService`, and existing order entities.
  - Command dedup uses `SagaEnvelope.eventId` in `saga_inbox`; reply publishing uses the reply envelope `eventId` as Kafka key so `SagaMessageConsumer` can dedup replies correctly.
  - Updated saga design docs with the implemented command handler flow.
- Verification:
  - `mvnw.cmd` is currently broken on this machine due a PowerShell wrapper null-array error, so Maven was run from the already downloaded wrapper distribution.
  - `~/.m2/wrapper/dists/apache-maven-3.9.11/.../bin/mvn.cmd -pl basecommerce -Dtest=SagaCommandHandlerServiceTest test` -> passed, 3 tests.
- Todo:
  - [ ] Add an integration test with embedded Kafka or Testcontainers for the full command -> reply -> orchestrator loop.
  - [ ] Decide whether command reply publishing should also use an outbox for stronger DB/Kafka atomicity.
- Notes:
  - Existing unrelated changes in `ecommerce-shop/common/src/main/java/com/ttl/common/dto/FulfillmentStatus.java`, `ecommerce-shop/common/src/main/java/com/ttl/common/dto/OrderStatus.java`, and untracked `ecommerce-shop/walkthrough.md` were left untouched.
