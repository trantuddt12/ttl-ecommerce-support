# 2026-05-10

## API Gateway design

### Scope: docs

#### Changed files
- `docs/api-gateway-design.md`

#### Summary
Thiết kế API Gateway cho project theo hướng tách module riêng, làm entrypoint duy nhất cho frontend, route về `basecommerce`/`searchservice`/`notificationservice`, và giữ gateway chỉ xử lý routing, security edge, rate limit, correlation id, observability, không chứa business logic. Tài liệu ghi rõ phase triển khai, route map, security model, testing checklist, và rủi ro để fresher có thể triển khai từng bước.

### Verification
Manual review nội dung markdown mới tạo; chưa chạy Maven vì chỉ thay đổi tài liệu.

### Risks
Route rewrite và JWT claim mapping phải đối chiếu lại khi triển khai code thật, nhất là path auth/product hiện tại và format claim role trong token.

### Next todos
- [ ] Tạo module `gatewayservice` trong `pom.xml`
- [ ] Chốt route rewrite theo path thực tế của controller hiện tại
- [ ] Cập nhật README và base URL frontend sang gateway

## Senior gateway learning

### Scope: docs

#### Changed files
- `docs/api-gateway-design.md`

#### Summary
Mở rộng tài liệu gateway thêm lớp senior: SLO/error budget, distributed tracing, contract testing, load test benchmark, canary rollout, RS256/JWKS, security hardening, và observability dashboard. Thêm code mẫu cho từng phần để fresher vừa nhìn kiến trúc vừa hình dung chỗ cần triển khai thật.

### Verification
Manual review markdown mới; không chạy Maven vì chỉ thay đổi tài liệu.

### Risks
Các phần senior là target design/learning path, chưa được code hóa trong repo; cần tách bước khi triển khai thật để tránh over-engineering.

### Next todos
- [ ] Chuyển SLO và tracing vào implementation plan nếu gateway thật sự được build
- [ ] Chốt contract test tool chọn dùng
- [ ] Ghi benchmark mục tiêu sau khi có gateway skeleton

